function getAllGames(req, res) {
  res.status(501).json({ message: "Pendiente por implementar" });
}

function getGameById(req, res) {
  res.status(501).json({ message: "Pendiente por implementar" });
}

function createGame(req, res) {
  res.status(501).json({ message: "Pendiente por implementar" });
}

function updateGame(req, res) {
  res.status(501).json({ message: "Pendiente por implementar" });
}

function deleteGame(req, res) {
  res.status(501).json({ message: "Pendiente por implementar" });
}

module.exports = {
  getAllGames,
  getGameById,
  createGame,
  updateGame,
  deleteGame,
};
